An outlined capsule used in conversion-flow rows, separated by → arrows. The current step is `active` (orange). Use `onDark` on the dark footer band.

```jsx
<Pill active onDark>Conversation</Pill> →
<Pill onDark>Qualified lead</Pill> →
<Pill onDark>Booked call</Pill>
```
